/** Keep in sync with `backend/src/config/expenseCategories.js` (display uses title case in UI). */
export const DEFAULT_EXPENSE_CATEGORIES = [
  "transport",
  "fuel",
  "rent",
  "salary",
  "utilities",
  "supplies",
];
